<%= form_for(@estandar) do |f| %>
  <% if @estandar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estandar.errors.count, "error") %> prohibited this estandar from being saved:</h2>

      <ul>
      <% @estandar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br>
    <%= f.text_field :tipo %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
